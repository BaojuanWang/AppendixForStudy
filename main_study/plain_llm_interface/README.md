# Plain LLM Interface

This directory contains the plain conversational LLM interface used in the main study.

## Contents

- `index.html`: learner-facing interface used during the LLM-assisted learning condition.

The interface is a standalone HTML/CSS/JavaScript implementation. It is preserved separately from VulnScaffold, which was developed later for the pilot evaluation using a different implementation structure.